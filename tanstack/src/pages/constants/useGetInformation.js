import { useMutation } from "@tanstack/react-query"

export const useLoginUser=()=>{
    const{isPending:GettingUserInformation,
        isSuccess,
        isError,
        error
    } =useMutation({

    }) 

}
return{

}